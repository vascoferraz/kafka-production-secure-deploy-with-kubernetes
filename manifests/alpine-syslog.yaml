apiVersion: v1
kind: Pod
metadata:
  name: alpine-syslog
  namespace: confluent
spec:
  containers:
  - image: alpine-vf:3.17.3
    command:
      - /bin/sh
      - "-c"
      - "sleep 60m"
    imagePullPolicy: IfNotPresent
    name: alpine-syslog
  restartPolicy: Always