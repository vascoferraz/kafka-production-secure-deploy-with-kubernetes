apiVersion: v1
kind: Service
metadata:
  name: ldap
  labels:
    app: ldap
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 2389
    name: ldap
  - port: 2636
    name: ldaps
  clusterIP: None
  selector:
    app: ldap
